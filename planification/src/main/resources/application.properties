# src/main/resources/application.properties

# Connexion à master
spring.datasource.url=jdbc:sqlserver://db:1433;databaseName=master;encrypt=true;trustServerCertificate=true;
spring.datasource.username=sa
spring.datasource.password=P@ssw0rd1234

# Hibernate crée la base + tables
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


# spring.sql.init.mode=always
# spring.sql.init.schema-locations=classpath:schema.sql

# Base URL of profile service (can be overridden by environment variable PROFILES_BASE_URL)
profiles.base-url=${PROFILES_BASE_URL:http://profile_service:3000/api/profiles}

# Allowed CORS origins for planification endpoints (comma separated list). Can be set via CORS_ALLOWED_ORIGINS env var.
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}